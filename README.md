# Note

These files goes into ```~/.config/nvim```  

● Install .NET 9.0 using ```./dotnet-install.sh --channel 9.0``` for ```csharp-language-package``` to work  
● In ```Mason``` find and install ```csharp-language-server``` and ```csharpier```  
● Run ```dotnet tool install --global csharp-ls``` just in case
